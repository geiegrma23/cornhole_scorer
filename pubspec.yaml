name: cornhole_scorer
description: A Flutter app for scoring cornhole games
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  # Firebase dependencies (for future use)
  # firebase_core: ^2.15.0
  # cloud_firestore: ^4.8.4
  # firebase_auth: ^4.7.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true